{% extends 'base.html.twig' %}
{% block body %}
<table class= "ui table">
	<tbody>
	{% for contact in contacts %}
		<tr>
			<td>{{ contact.nom }}</td>
			<td><a href="{{ path("contact_edit",{"index": loop.index}) }}" class="ui icon button xs"></a></td>
		</tr>
	{% endfor %}
</table>
{% endblock %}